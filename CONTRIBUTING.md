# 三人小组的功能开发与集成流程

根 README 负责快速入门，`AGENTS.md` 负责约束 Agent，本文件解释日常怎样协作。普通功能遵循分支和 PR 流程；初始化、维护或合并已有当前人类明确授权时，按该授权执行并保留记录。

## 1. 人和 Agent 各自负责什么

| 参与者           | 负责的事情                                                    |
| ---------------- | ------------------------------------------------------------- |
| 功能负责人（人） | 提出可验收的功能、写提示词、做取舍、实际试玩、对结果负责      |
| 该成员的 Agent   | 检查上下文、实现、测试、记录、在授权范围内维护 Issue 和 PR    |
| 另一名组员（人） | 从玩家和集成角度评审，确认不会破坏已有功能                    |
| 评审者的 Agent   | 协助阅读 diff、检查代码与回归风险；不能冒充人类已体验或已同意 |

评审人按任务轮换，不要求固定角色。拥有仓库管理员权限不等于可以跳过任务边界。

## 2. 从一个想法开始

在 [Issues](https://github.com/keying-s/github-collaboration-homework/issues) 查重，再使用“功能提案”模板创建任务。

最少说明：

- **玩家价值**：什么场景下会更好玩或更顺畅？
- **验收条件**：别人如何判断已经做对？例如“冲锋前有可见预警”“伤害只结算一次”。
- **范围和非目标**：这次做什么，不顺带做什么。
- **负责人**：使用 Issue assignee；无权限设置时，在描述中明确写出负责人，由有权限成员更新。
- **相关工作**：查到的重叠 Issue / PR、共享接口和先后依赖。

不要求每个提案先写长文档。任务大到无法在一个易评审的 PR 中完成时，再拆成里程碑；复杂设计使用 [功能文档模板](docs/FEATURE_TEMPLATE.md)。

## 3. 同步并建立自己的分支

先确认工作目录没有尚未处理的修改。以下示例从仓库根目录运行；把示例用户名和分支名替换成自己的。

```powershell
git status --short --branch
git fetch origin
git switch main
git pull --ff-only origin main
git switch -c feat/keying-s/splitting-enemy
```

`--ff-only` 失败时先检查本地分叉原因，不用 `reset --hard` 解决。如果当前已有本任务分支，继续原分支即可。

如果同一台电脑已有另一个 Agent 在开发，用另一份 clone 或 worktree。先 `git fetch origin`，再例如：

```powershell
git worktree add ../homework-splitting-enemy -b feat/keying-s/splitting-enemy origin/main
```

两个示例是二选一，不能重复创建同名分支。每个 worktree 分别安装依赖。需要同时运行多个开发服务器时，在游戏目录执行 `node node_modules/vite/bin/vite.js --port 5174`，选择空闲端口；不要关闭别人的服务来抢端口。

## 4. 编码前说明影响范围

在 Issue 或功能文档记录预计涉及的文件。下表 `game/`、`ui/`、`style.css` 均位于 `ember-protocol/src/` 下，构建文件位于游戏根目录。大致这样分：

| 本次功能                 | 通常会触及                                    | 需要留意                               |
| ------------------------ | --------------------------------------------- | -------------------------------------- |
| 增加房间或调整波次       | `game/config.ts`                              | 现有流程测试中的波次数和敌人数假设     |
| 新武器 / 技能 / 怪物机制 | `game/types.ts`、`config.ts`、`simulation.ts` | 公共类型、现有机制组合和渲染支持       |
| 命中反馈和美术           | `game/renderer.ts`、`audio.ts`                | 不能用视觉效果代替真实战斗行为         |
| 菜单、HUD 和可访问性     | `ui/interface.ts`、`style.css`                | 暂停、输入焦点、窄窗口布局和文案真实性 |
| 按键与控制方式           | `game/scene.ts`                               | 短按缓冲、失焦暂停和旧键位             |
| 依赖、构建、CI           | `package.json`、锁文件、Vite 配置、workflow   | 影响所有成员，避免多个 PR 同时升级     |

**示例：A 在加冰冻枪，B 在加护盾怪。** 两人都会碰到伤害规则时，先确定伤害与状态效果接口；可以先合并一个小的接口 PR，再分别实现内容。不要各自重写整个 `Simulation`，最后靠选择其中一个文件解决冲突。

若只是同一配置文件中的不同条目，可以保持小 diff 并正常开发；不需要把每个共享文件都当成禁止修改的锁。

## 5. 实现、验证、提交

- 尽量形成小而完整的改动，保留原有行为。
- 给行为变化增加适当回归测试；UI 变化检查实际画面和交互。
- 在游戏目录运行 `npm.cmd test`、`npm.cmd run build`、`npm.cmd run format:check`。
- 依赖改变时用 npm 更新锁文件，确认 `npm.cmd ci` 可复现安装。
- 更新受影响的文档和说明。保存关键提示词的**摘要**、人类追加约束及最终验收，不必上传整段聊天，更不能附带密钥或私人内容。

检查 diff 后明确指定本次要提交的文件，不盲目 `git add .` 把未知文件带入仓库。提交信息建议：

```text
feat: add splitting crawler enemy
fix: prevent duplicate damage during dash
docs: explain weapon extension workflow
```

使用自己的 GitHub 身份，不冒用队友。代码由 Agent 辅助编写可以在 PR 说明；不要伪造别人参与、签名或确认。

## 6. 推送并建立 PR

在已获得该任务 GitHub 写入授权时：

```powershell
git push -u origin feat/keying-s/splitting-enemy
```

在 GitHub 创建以 `main` 为目标的 PR。描述会自动提供模板，填入：

- 关联 Issue、功能负责人。
- 之前的问题与现在玩家看到的结果。
- 改了哪些模块、是否有公共接口或依赖变更。
- 测试命令和实际结果，相关试玩步骤及截图（视觉变化适用）。
- 已知限制、未验证的环境、需要评审者重点看的内容。
- 简短的提示词 / 人类决策摘要，让作业过程可追溯。

未完成的 PR 标为 Draft。只有确实完成 Issue 全部验收时才使用 `Closes #编号`；分阶段工作用 `Refs #编号`。

## 7. 同步主线与解决冲突

已推送的功能分支默认合并最新主线，不通过 rebase 强制重写他人可能已经检出的历史：

```powershell
git status --short --branch
git fetch origin
git merge origin/main
```

如果出现冲突：

1. 记录冲突文件、双方在做的功能；如本次作业要求截图，在解决前保存冲突状态截图。
2. 阅读双方提交和关联 Issue，保留双方有效意图，不盲选整个文件的 `ours` / `theirs`。
3. 产品规则矛盾时请双方负责人做选择，普通文本或实现冲突由 Agent 处理。
4. 修复代码，检查无遗留冲突标记和未合并文件；重新跑相关测试与试玩。
5. 暂存解决后的相关文件，完成 merge commit，正常 push；在 PR 说明合并了什么、验证了什么。

`git merge --abort` 可以退出**本次未完成的合并**，但使用前仍需检查工作区。不要用删除整个仓库或覆盖文件的方式清除冲突。若同一远端功能分支被别人推进，也先获取并理解那些提交，不能强推抢回分支。

## 8. 评审和合并

普通功能由至少另一名组员查看，建议轮流负责。评审时回答：

- 是否满足验收？玩家能理解吗？
- 是否保留了已有机制，和已合入功能能同时工作？
- 是否破坏层次边界，制造了重复状态或难以维护的抽象？
- 测试和文档是否与真正实现一致？
- 是否有不必要的大改、依赖、生成文件或敏感内容？

Game CI 会在相关 PR 和 `main` 更新上运行 Windows / Ubuntu 的测试、构建与格式检查。依赖锁文件是安装依据。按需更新分支后必须等待最新提交的检查结果，不能拿旧提交通过的结果代替。

获得当前任务的合并授权、必要的队友评审及相关检查通过后，推荐 Squash merge，让主线保留“一项功能一个可追溯提交”。不要使用管理员绕过失败检查。当前文档不代表已经配置了 GitHub 的强制分支保护。

合并后：关联 Issue 更新结果，三位成员各自同步 `main`。仍在进行中的功能分支合并最新主线，并验证组合行为。只有确认不再需要且获得授权，才清理相应分支或 worktree。

## 9. 避免为了协作而制造更多冲突

- 不把所有任务状态写到同一个 `team-notes.md`；使用 Issue assignee、PR 和独立功能文档。
- 不要求每个功能都更新根 README；只更新实际发生变化的公共说明。
- 不因发现无关问题顺手大改；把它记为后续任务。
- 不在缺少授权时擅自发消息、评论、合并、部署或更改仓库权限。
- 已有明确授权的正常开发步骤就继续完成，不让人类反复确认同一件事。
